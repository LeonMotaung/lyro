{
  "name": "lyro-react",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "node App.js",
    "dev": "cd vite && npm run dev",
    "build": "cd vite && npm run build",
    "install:all": "npm install && cd vite && npm install && cd ..",
    "backend": "node backend/server.js"
  },
  "dependencies": {
    "concurrently": "^9.2.1",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "mongodb": "^7.0.0"
  }
}
